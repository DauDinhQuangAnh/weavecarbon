import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";describe("G2-04 mitigation operations workspace",()=>{const source=readFileSync(resolve(process.cwd(),"components/dashboard/mitigation-operations/MitigationOperationsClient.tsx"),"utf8");it("connects the full initiative-to-position chain",()=>{expect(source).toContain("createInitiative");expect(source).toContain("createScenario");expect(source).toContain("createAllocation");expect(source).toContain("createPosition");});it("keeps gross emissions visible and states the legal boundary",()=>{expect(source).toContain("grossEmissionsTco2e");expect(source).toContain("legalNotice");});});
