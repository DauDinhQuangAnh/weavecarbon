import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";describe("G2-03 VN MRV workspace",()=>{const s=readFileSync(resolve(process.cwd(),"components/dashboard/vn-mrv/VnMrvClient.tsx"),"utf8");it("connects applicability, plans, inventories and filing snapshots",()=>{expect(s).toContain("vnMrvApi.createCase");expect(s).toContain("vnMrvApi.createPlan");expect(s).toContain("fetchCorporateGhgInventories");expect(s).toContain("vnMrvApi.prepareFiling");});it("states the non-submission boundary",()=>expect(s).toContain("legalNotice"));});
